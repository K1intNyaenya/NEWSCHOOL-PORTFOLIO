version: "3.9"

services:
  backend:
    # Override DB to use host MySQL in development
    depends_on: []
    environment:
      DB_HOST: "host.docker.internal"
      DB_PORT: "3306"
    extra_hosts:
      - "host.docker.internal:host-gateway"

